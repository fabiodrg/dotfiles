# DNF

DNF has a global configuration file. The contents in `dnf.conf` are just my settings and should be appended to `/etc/dnf/dnf.conf`.

Documentation available at: https://dnf.readthedocs.io/en/latest/index.html
